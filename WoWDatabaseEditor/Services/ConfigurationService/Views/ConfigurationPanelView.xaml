<UserControl x:Class="WoWDatabaseEditor.Services.ConfigurationService.Views.ConfigurationPanelView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:Metro="http://metro.mahapps.com/winfx/xaml/controls"
                   xmlns:prism="http://prismlibrary.com/"
                   xmlns:utils="clr-namespace:WoWDatabaseEditor.Utils"
                   xmlns:system="clr-namespace:System;assembly=System.Runtime"
                   xmlns:utils1="clr-namespace:WDE.Common.Utils;assembly=WDE.Common"
                   prism:ViewModelLocator.AutoWireViewModel="False">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TabControl ItemsSource="{Binding ContainerTabItems}" SelectedItem="{Binding SelectedTabItem}"
                    TabStripPlacement="Left">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsModified}">
                                        <DataTrigger.Value><system:Boolean>True</system:Boolean></DataTrigger.Value>
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ContentControl utils1:ViewBind.Model="{Binding}" HorizontalContentAlignment="Stretch"
                                    VerticalContentAlignment="Stretch" />
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
        <Button Margin="0,10,0,0" Content="Save" Grid.Row="1" HorizontalAlignment="Left" Width="119"
                Command="{Binding Save}" />
    </Grid>
</UserControl>