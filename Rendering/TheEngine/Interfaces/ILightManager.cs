namespace TheEngine.Interfaces
{
    // Lights are ECS Light components gathered by LightManager; fog moved to ICamera.Fog. This
    // interface is now just the public handle exposed via Engine.LightManager.
    public interface ILightManager
    {
    }
}
